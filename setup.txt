cd Dev
dotnet new console -o project-name
cd project-name
code .

compile and run

## (inside vscode term)
-dotnet build

## (in bash) 
-cd into project-name
- dotnet run

## check dotnet ver
dotnet --list-sdks


gif-cli.runtimeconfig.json

{
  "runtimeOptions": {
    "tfm": "net6.0",
    "framework": {
      "name": "Microsoft.NETCore.App",
      "version": "6.0.0"
    },
    "configProperties": {
      "System.Drawing.EnableUnixSupport": true
    }
  }
}
